<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
  
      <%= link_to "poker trainer", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%#= link_to "Help", help_path %></li>
          <% if signed_in? %>
              <li class="white">Hello, <%= current_user.email %>! You have <%= current_user.credits %> credits.</li>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  Account <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Profile", profile_path %></li>
                  <li><%= link_to "Settings", edit_user_registration_path(current_user) %></li>
                  <li><%= link_to "Sign out", destroy_user_session_path, method: "delete" %></li>
                  <li class="divider"></li>
                  <li><%= link_to "Create game", new_game_path%></li>

                </ul>
              </li>
          <% else %>
              <li><%= link_to "Sign in", new_user_session_path %></li>
          <% end %>
        </ul>
      </nav>
   
    </div>
  </div>
</header>